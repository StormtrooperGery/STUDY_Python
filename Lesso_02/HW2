easy_code = [float(x) for x in input("Введите 2 числа: ").split()]
easy_code.sort()
if easy_code[-1] % 1 > 0:
    easy_code[-1] = easy_code[-1]+1
easy_code2 = list(range(int(easy_code[0]), int(easy_code[1])))
pos = []
for x in easy_code2:
    if x > 0:
        pos.append(x)
        continue
print(pos)

#Над этим кодом работали вместе с Константиновым
